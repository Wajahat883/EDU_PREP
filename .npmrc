# Suppress known deprecation warnings from transitive dependencies
# These come from tools like eslint, glob, rimraf which are in the dependency chain
# but don't affect production code
legacy-peer-deps=true
fund=false
audit=false
